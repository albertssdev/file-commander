{
  "SessionStart": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "command",
          "command": "python -c \"import sys, json; ok=sys.version_info>=(3,9); out={'decision':'approve','reason':f'WARNING - File Commander requires Python 3.9+ (found {sys.version.split()[0]}) - upgrade at https://python.org'} if not ok else {'decision':'approve'}; print(json.dumps(out))\"",
          "timeout": 10
        }
      ]
    }
  ]
}
